$('#btn-collect-<%= @clip.id %>').remove();
$('.count-info.collect-clip.<%=@clip.id%>').append("<%= escape_javascript render '_partials/collect_button', :clip => @clip, :url => add_collect_clip_path(p), :style => 'btn btn-success', :str => '收藏' %>");
